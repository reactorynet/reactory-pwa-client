# form-engine

Reactory's adapter layer over upstream `react-jsonschema-form` v5. See
`../docs/forms-engine/` for the design and migration plan.

This directory is empty by design at the start of Phase 1. New work lands here
under the test net described in `../docs/forms-engine/09-test-strategy.md`.
