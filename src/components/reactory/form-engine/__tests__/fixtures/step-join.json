{
  "name": "step-join",
  "description": "Join multiple branches back together",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "title": "Step Name",
        "description": "Name of the workflow step",
        "default": "Join"
      },
      "waitForAll": {
        "type": "boolean",
        "title": "Wait for All Branches",
        "description": "Wait for all branches to complete before continuing",
        "default": true
      }
    },
    "required": ["name"]
  },
  "uiSchema": {
    "waitForAll": {
      "ui:help": "Enabled: waits for every branch to finish before proceeding. Disabled: continues as soon as the first branch completes."
    }
  },
  "formData": {
    "name": "Join",
    "waitForAll": true
  },
  "source": "WorkflowDesigner/Steps/Join/definition.ts"
}
