import fields from './fields';
import * as widgets from './widgets';
import templates from './templates';

const MuiReactoryPackage = {
  fields,
  widgets,
  templates
}

export default MuiReactoryPackage;