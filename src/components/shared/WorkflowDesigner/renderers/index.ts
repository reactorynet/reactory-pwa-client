/**
 * Workflow Designer Renderers
 * 
 * This module exports different rendering implementations for the Workflow Designer.
 * 
 * Available renderers:
 * - DOM/SVG (default): Uses React components and SVG for connections
 * - WebGL: Uses Three.js for GPU-accelerated rendering
 */

// WebGL Renderer
export * from './WebGLRenderer';
