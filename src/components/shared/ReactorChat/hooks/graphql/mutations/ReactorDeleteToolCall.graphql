mutation ReactorDeleteToolCall($chatSessionId: String!, $toolCallId: String!, $messageId: String) {
  ReactorDeleteToolCall(chatSessionId: $chatSessionId, toolCallId: $toolCallId, messageId: $messageId)
}
