mutation ReactorSetChatMaxToolIterations($chatSessionId: String!, $maxToolIterations: Int!) {
  ReactorSetChatMaxToolIterations(chatSessionId: $chatSessionId, maxToolIterations: $maxToolIterations) {
    id
    maxToolIterations
  }
}
