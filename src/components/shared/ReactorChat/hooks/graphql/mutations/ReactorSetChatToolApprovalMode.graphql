mutation ReactorSetChatToolApprovalMode($chatSessionId: String!, $mode: ReactorToolApprovalMode!) {
  ReactorSetChatToolApprovalMode(chatSessionId: $chatSessionId, mode: $mode) {
    id
    toolApprovalMode
  }
}

