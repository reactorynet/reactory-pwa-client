mutation ReactorCompactConversation($chatSessionId: String!) {
  ReactorCompactConversation(chatSessionId: $chatSessionId) {
    success
    messagesArchived
    tokensBefore
    tokensAfter
    usedFallback
    error
  }
}
