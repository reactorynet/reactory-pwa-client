import React from 'react';
const {
    useState
} = React;

const FormEditor = (props) => {


    return (
        <React.Fragment>
            <p>Form Editor { name }</p>
        </React.Fragment>
    )
}

export default FormEditor;