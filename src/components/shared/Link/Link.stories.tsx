import React from "react"; import type { Meta, StoryObj } from "@storybook/react"; import { ThemeWrapper } from "@reactory/client-storybook/ThemeWrapper"; const MockLink = ({ link = "/example", linkTitle = "Example Link", uiSchema = {} }: any) => { const linkText = link; const linkTitleText = linkTitle; const linkIcon = uiSchema["ui:options"]?.icon ? <span style={{ marginLeft: "8px" }}>🔗</span> : null; return (<div style={{ padding: "16px" }}><div style={{ border: "1px solid #e0e0e0", borderRadius: "8px", padding: "16px", backgroundColor: "#fafafa" }}><a href={linkText} style={{ color: "#1976d2", textDecoration: "underline" }}>{linkTitleText}{linkIcon}</a></div></div>); }; const meta = { title: "Shared/Link", component: MockLink, decorators: [(Story) => (<ThemeWrapper showThemeSelector={false}><Story /></ThemeWrapper>)] } satisfies Meta<typeof MockLink>; export default meta; type Story = StoryObj<typeof meta>; export const Default: Story = { args: { link: "/example", linkTitle: "Example Link" } }; export const WithIcon: Story = { args: { link: "/example", linkTitle: "Example Link", uiSchema: { "ui:options": { icon: "link" } } } };
