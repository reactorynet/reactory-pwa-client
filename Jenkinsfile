pipeline {
    agent any
    dir('/mnt/d/data/builds') {
      stages {       
        stage('Build') {          
          // some block
          steps {
            sh 'npm install' 
          }
        }                  
      }
    }    
}